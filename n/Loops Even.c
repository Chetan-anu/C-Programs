#include<stdio.h>
int main()
{
    int i,Sum=0,n;
    printf("Enter The Value=");
    for ( i=0; i<n; i++)
    scanf("%d", &n);
    {
        if(i%2==0)
        Sum=Sum+i;
    }
    printf("%d",Sum);
    return 0;
}