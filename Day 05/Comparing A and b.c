#include<stdio.h>
int main()
{
    int a,b,sum
    printf("Enter The Value oF a,b;");
    scanf("%d%d", &a, &b);
     sum:a>b
     printf("The sum Of %d%d is %d\n", a,b,sum);
     if (a=1)
     {
        printf("The biggest No. is a");
        return 0;
     }
     
}